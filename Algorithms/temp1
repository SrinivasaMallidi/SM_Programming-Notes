
    # Define the absolute path where you want to save the file
    output_directory = "/path/to/your/directory/"

    # Generate the file name with the absolute path
    output_file_name = os.path.join(output_directory, datetime.now().strftime("%Y-%m-%d_%H-%M-%S_servers.txt"))
